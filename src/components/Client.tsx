import React from "react";
import { costumer } from "../types/costumer";

interface props{
    costumer:costumer
}
const Client:React.FC<props>=({costumer})=>{
    return(
        <div>
        </div>
    )
}
export default Client